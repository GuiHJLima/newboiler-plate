import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    
  textButton:{
    fontSize: 20,
    fontStyle: 'italic',
    fontWeight: 'bold',
    padding: 10,
  },
    });

export default styles;